# Rust
/target/
Cargo.lock

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Logs
*.log

# Environment
.env
.env.local

# Security
*.pem
*.key
*.crt

# Build artifacts
*.tar.gz
*.zip

# Coverage reports
tarpaulin-report.html
cobertura.xml

# Benchmarks
criterion/

# Local data
*.json
!examples/*.json
!data/*.json
# v0.2 eval framework: keep the config files, ignore the per-run outputs.
!eval/repos.json
!eval/threshold.json
eval/results/
# Goldsets: hand-curated set is checked in (.jsonl), auto-generated ones are not.
eval/goldset/*.jsonl
!eval/goldset/handcurated.jsonl
# v0.2 harness transcripts: per-run outputs only, never committed.
tests/harness/transcripts/

# Docker
.dockerignore

# Kubernetes secrets
k8s/secrets/
